import styled from "styled-components";

export const Form = styled.form`
  display: flex;
  gap: 1rem;
  flex-direction: column;

  h1 {
    font-weight: 600;
    margin-bottom: 1rem;
  }

  button {
    display: inline;
    align-self: flex-end;
    margin-top: 1.5rem;
  }
`;
